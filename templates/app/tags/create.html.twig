{% extends '@layout/default.html.twig' %}

{% block title %}Cadastro de Tags - {{ parent() }}{% endblock %}

{% block page_header %}Cadastro de Tags{% endblock %}

{% block content %}
	{% include '@app/tags/_form-elements.html.twig' %}

	{% set submit = form.get('submit') %}
	{% do submit.setAttribute('class', 'btn btn-primary').setLabel('Incluir') %}

	{% do form.setAttribute('action', url('admin.tags.create')) %}
	{% do form.prepare() %}

	<div class="row">
		<div class="col-md-8">
			<div class="box box-primary">
				<div class="box-body">
					{{ form().openTag(form) | raw }}
						{% include '@app/tags/_form-body.html.twig' %}
						<div class="box-footer">
							{{ formButton(submit) }}
						</div>
					{{ form().closeTag() | raw }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}